public class Sudoku9x9 {

    /*
        Implementare due costruttori
        uno senza argomenti, che inizializza tutti i numeri a 0 (significa casella vuota)
        e un'altro che riceve una variabile di tipo int[][] con il sudoku iniziale
        questo deve essere: della dimensione giusta, con le sottomatrici delle dimensioni giuste,
        e i numeri inseriti devono non andare in conflitto con le regole del sudoku
    */


    /*
        implementa 3 metodi di check
        - checkRiga
        - checkColonna
        - checkArea
        Ognuno controlla che una certa riga o colonna sia valida 
        e ciascuno ha un solo parametro in ingresso (int, 1-9)
        e tutte ritornano boolean
        lancia un eccezione appropriata se gli indici sono fuori range

        implementa poi un metodo addNumber(int x, int y, int n)
        tutti e tre devono andare a 1 a 9
        lancia un eccezione appropriata se gli indici sono fuori range
        o se la casella era già occupata
        torna true se l'ha aggiunto, 
        o false se non lha aggiunto perche' non rispetta le regole del sudoku (deve fare tutti e tre i test)

        implementa un metodo chiamato daiStato()
        che torna una copia della matrice attuale dentro il sudoku
        ATTENZIONE deve essere un oggetto nuovo, non la matrice interna al sudoku
    */

}
